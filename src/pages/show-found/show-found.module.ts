import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ShowFoundPage } from './show-found';

@NgModule({
  declarations: [
    ShowFoundPage,
  ],
  imports: [
    IonicPageModule.forChild(ShowFoundPage),
  ],
})
export class ShowFoundPageModule {}
